pipeline {
    
    agent any 
    
    environment {
        IMAGE_TAG = "${BUILD_NUMBER}"
     }

    stages {

     stage('Checkout'){
           steps {
           git branch: 'master',
           url: '/home/shyamlal/repositories/cicd-pythonapp/'           
}
        }

    stage('load') {
      steps {
       sh 'cat /proc/cpuinfo'
       sh 'cat /etc/os-release'
      }
    }
}
}
