pipeline {
    
    agent any 
    
    environment {
        IMAGE_TAG = "${BUILD_NUMBER}"
     }

    stages {

     stage('Checkout'){
           steps {
                url: '/home/shyamlal/repositories/cicd-pythonapp/',
                branch: 'master'
           }
        }

    stage('load') {
      steps {
       sh 'cat /proc/cpuinfo'
       sh 'cat /etc/os-release'
      }
    }
}
}
